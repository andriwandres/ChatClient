import { NgModule } from '@angular/core';
import { StoreModule } from '@ngrx/store';
import { AUTH_FEATURE_KEY } from './+store/state';



@NgModule({
  declarations: [],
  imports: [
    // StoreModule.forFeature(AUTH_FEATURE_KEY, )
  ]
})
export class AuthStoreModule { }
